# Queries

Store shared SQL query files here when backend modules begin using persisted data.

Guidelines:
- Keep queries grouped by feature module.
- Prefer parameterized SQL.
- Avoid mixing business logic into SQL files.
- Keep workspace filters explicit in tenant-scoped queries.

No dashboard or CRM queries are active yet.
